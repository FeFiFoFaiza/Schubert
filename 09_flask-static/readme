-Flask serves the static webpages under the /static/ directory they are put into 
in the file hierarchy of our project directory
-html files are served as expected in a webpage
-files with no extension are downloaded, this probably extends to most 
or all files that are not .html