1. Figure how to automatically get request from nasa via computar 
    ACCOMPLISH THIS VIA requests.get()
2. Convert to python dict or JSON
    Once you get requests object, you can call the .json() method
3. Reference this image and render this image 
    USE JINJA? -- Placeholders
